#include <stdio.h>

int main (void) {
        int age;
        puts("Bonjour,");
        puts("Quel age avez vous ?");
        scanf ("%d", &age);
        printf("Vous avez %d ans.\n", age);
        return 0;
}
